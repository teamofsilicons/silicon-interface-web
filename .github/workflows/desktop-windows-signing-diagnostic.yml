name: Desktop Windows signing diagnostic

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  certificate:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-2022, windows-11-arm]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Exercise engineering certificate primitives
        shell: pwsh
        timeout-minutes: 3
        run: |
          $ErrorActionPreference = 'Stop'
          Write-Host 'checkpoint: start'
          $publisher = 'Silicon Interface Engineering CI'
          $passwordBytes = New-Object byte[] 32
          [System.Security.Cryptography.RandomNumberGenerator]::Fill($passwordBytes)
          $passwordText = [Convert]::ToBase64String($passwordBytes)
          Write-Output "::add-mask::$passwordText"
          Write-Host 'checkpoint: random'
          $pfxPath = Join-Path $env:RUNNER_TEMP 'silicon-engineering-signing.pfx'
          $cerPath = Join-Path $env:RUNNER_TEMP 'silicon-engineering-signing.cer'
          $rsa = [System.Security.Cryptography.RSA]::Create(2048)
          Write-Host 'checkpoint: rsa'
          $request = [System.Security.Cryptography.X509Certificates.CertificateRequest]::new(
            "CN=$publisher",
            $rsa,
            [System.Security.Cryptography.HashAlgorithmName]::SHA256,
            [System.Security.Cryptography.RSASignaturePadding]::Pkcs1
          )
          $request.CertificateExtensions.Add(
            [System.Security.Cryptography.X509Certificates.X509KeyUsageExtension]::new(
              [System.Security.Cryptography.X509Certificates.X509KeyUsageFlags]::DigitalSignature,
              $true
            )
          )
          $enhancedUsages = [System.Security.Cryptography.OidCollection]::new()
          [void]$enhancedUsages.Add(
            [System.Security.Cryptography.Oid]::new('1.3.6.1.5.5.7.3.3', 'Code Signing')
          )
          $request.CertificateExtensions.Add(
            [System.Security.Cryptography.X509Certificates.X509EnhancedKeyUsageExtension]::new(
              $enhancedUsages,
              $true
            )
          )
          $request.CertificateExtensions.Add(
            [System.Security.Cryptography.X509Certificates.X509SubjectKeyIdentifierExtension]::new(
              $request.PublicKey,
              $false
            )
          )
          Write-Host 'checkpoint: request'
          $certificate = $request.CreateSelfSigned(
            [DateTimeOffset]::UtcNow.AddMinutes(-5),
            [DateTimeOffset]::UtcNow.AddDays(2)
          )
          Write-Host 'checkpoint: self-signed'
          [IO.File]::WriteAllBytes(
            $pfxPath,
            $certificate.Export(
              [System.Security.Cryptography.X509Certificates.X509ContentType]::Pfx,
              $passwordText
            )
          )
          Write-Host 'checkpoint: pfx'
          [IO.File]::WriteAllBytes(
            $cerPath,
            $certificate.Export([System.Security.Cryptography.X509Certificates.X509ContentType]::Cert)
          )
          Write-Host 'checkpoint: cer'
          $publicCertificate = [System.Security.Cryptography.X509Certificates.X509Certificate2]::new($cerPath)
          foreach ($storeName in @('Root', 'TrustedPublisher')) {
            Write-Host "checkpoint: opening $storeName"
            $store = [System.Security.Cryptography.X509Certificates.X509Store]::new(
              $storeName,
              [System.Security.Cryptography.X509Certificates.StoreLocation]::CurrentUser
            )
            try {
              $store.Open([System.Security.Cryptography.X509Certificates.OpenFlags]::ReadWrite)
              Write-Host "checkpoint: adding $storeName"
              $store.Add($publicCertificate)
              Write-Host "checkpoint: added $storeName"
            } finally {
              $store.Close()
            }
          }
          Write-Host 'checkpoint: PASS'
